# Room schemas

O build Android deve exportar aqui o schema versionado da outbox. O primeiro
arquivo JSON será gerado quando o projeto for sincronizado em um ambiente com
Android SDK e Gradle disponíveis.
